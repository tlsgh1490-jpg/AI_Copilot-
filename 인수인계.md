# Coke Oven Gas AI Copilot 과제 인수인계

> 최종 갱신: 2026-08-05
>
> 이 문서는 과제 맥락, 확정된 결정, 산출물, 다음 할 일을 한 곳에 모은 기준 문서입니다. 새 Codex 작업 또는 다른 계정에서 프로젝트를 이어갈 때 먼저 읽습니다.

## 1. 과제 목적

- **대상:** 제철소 Coke Oven Gas 공정의 단일 라인
- **목적:** 공정 엔지니어가 주요 KPI를 모니터링하고, 이상 후보·예상 원인·점검 우선순위·원가 영향·유사 과거 사례를 근거와 함께 확인하도록 지원하는 AI Copilot
- **AI 역할:** 운전을 대신하거나 실제 원인을 확정하지 않는다. 이상 후보와 유사 사례에 기반한 점검 후보를 제시하고, 현업이 판단·확정한다.

## 2. 현재 최우선 목표

**구현·대시보드보다 먼저, 단일 라인의 주요 KPI 중심 더미데이터를 설계·생성한다.**

- 기간: 가상 날짜 기준 1년
- 공정 데이터: 1시간 단위(약 8,760행/변수)
- 시작 범위: 주요 변수 15~25개, KPI 5~10개
- 생성 방식: Codex가 공정 관계·생성 규칙·이상 시나리오를 설계하고 Python 생성기를 작성·실행해 수치 데이터를 생성한다. 대화에서 수치 행을 직접 나열하지 않는다.
- 이상 데이터: 정상 데이터 위에 시나리오를 삽입한다.
- 시나리오 흐름: 정상 → 이상 전조 → 이상 발생 → 조치/정비 → 회복

## 3. 1차 MVP 범위

1. 주요 KPI 모니터링
2. 정상 기준 기반 이상 탐지
3. 예상 원인 및 영향 변수 제시
4. 원가 영향 계산
5. 구조화된 과거 이상·정비 사례 검색 및 유사 사례 추천
6. Morning Brief 자동 생성
7. 수기 입력한 가상 계약·경영계획 목표 대비 실적 분석

후속 확장 범위는 실제 정비이력 DB, 운영계획 연계, 계약서 자동화, 실시간 시스템 연계·알림, 다중 라인/공정 확장, 고도화 모델이다.

## 4. 보안·데이터 원칙

- 원본 행 데이터, 실제 날짜·시간, 실제 설비명·태그명, 계약번호·고객명, 실제 단가는 사용·공유하지 않는다.
- 변수·설비·공정은 `gas_flow_01`, `compressor_01`, `process_01`처럼 익명화한다.
- 정확한 평균·중앙값·분위수·상관계수보다 범주형 정보를 우선 사용한다.
- 관계 강도는 `매우 강함/강함/중간/약함/모름`처럼 입력한다.
- 시간 지연은 `즉시/1~3시간/수시간/반나절/다음날/모름`처럼 구간으로 입력한다.
- 원가는 가상 단가 또는 기준지수 100을 사용한다.
- 정상·경고·이상 범위는 초기에는 `낮음/보통/높음` 등 일반화된 표현을 허용한다.

## 5. 정상 기준·이상 탐지 원칙

- 최근 1년 데이터의 정상 운전 구간을 중심으로 정상 기준을 만든다.
- 정기수리, 기동·정지, 명확한 이상, 센서 오류·결측 구간은 정상 기준 분석에서 분리·제외한다.
- 초기에는 복잡한 ML보다 통계·규칙 기반 이상 탐지를 우선한다.
- 실제 운영에서는 AI가 이상 후보를 찾고 현업이 실제 이상 여부를 확정한다.

## 5-1. 조업일 집계 기준

- 공정 원시 데이터는 1시간 단위로 생성·분석한다.
- 일 단위 KPI, 생산량, 원가 등은 달력일(00:00~24:00)이 아니라 **당일 07:00부터 익일 07:00까지의 24시간**을 하나의 조업일로 집계한다.
- 더미데이터 생성기, KPI 집계 로직, 대시보드 일별 조회, Morning Brief에 동일 기준을 적용한다.

## 6. 과거 사례 추천 원칙

현재 이벤트와 과거 이벤트를 같은 구조로 요약해 유사도를 비교한다.

- 변수별 정상 대비 변화 방향·크기
- 지속시간과 시간 지연
- 영향 KPI 및 원가 영향
- 이상 유형·추정 원인

표현은 반드시 **“유사 사례 및 점검 후보”**로 제한한다. 합성 시나리오만으로 실제 원인을 확정하지 않는다.

## 7. 현재 산출물

| 항목 | 위치 | 상태 |
| --- | --- | --- |
| 더미데이터 입력 템플릿(기준본) | `엑셀 양식/coke_oven_gas_dummy_data_input_template_revised.xlsx` | 작성 대기 |
| 본 인수인계 | `인수인계.md` | 지속 갱신 |

### 파일 관리 방식

- 프로젝트 최상위 폴더의 `인수인계.md`를 기준 문서로 유지한다.
- 엑셀 등 산출물은 목적별 하위 폴더에 개별 파일로 보관한다.
- 별도 ZIP 묶음 파일은 만들거나 관리하지 않는다.
- 다른 계정에서 작업할 때도 동일한 프로젝트 폴더를 열고 이 문서부터 확인한다.

### 엑셀 템플릿 구성

1. `시작안내`
2. `KPI_목록`
3. `변수_목록`
4. `변수관계`
5. `이상시나리오`
6. `원가_계산식`
7. `이상실적_이력`
8. `생성설정`
9. `입력예시_삭제` — 작성 참고용이며, 입력을 마친 뒤 삭제 가능
10. `KPI_변수_매핑` — KPI·변수 겸용 항목을 중복 없이 연결

템플릿 내 `이상실적_이력`과 `생성설정`의 제목 번호는 각각 6, 7로 수정 완료했다.

## 8. 사용자의 다음 입력 순서

1. KPI 목록: 5~10개
2. KPI별 주요 변수 목록: 15~25개
3. 핵심 변수 관계: 약 10~20개
4. 핵심 변수 분포 특성: 분포 형태, 비대칭, 변동성, 변화 속도, 주기성
5. 이상 시나리오: 3~5개
6. 원가 계산식: 2~3개
7. 과거 이상실적 요약: 있으면 추가
8. 생성 설정: 기간, 주기, 정상/이상 비율, 이벤트 수

## 9. 과제 완료 로드맵

| 단계 | 작업 | 진도 비중 | 완료 기준 |
| --- | --- | ---: | --- |
| 1 | **데이터 기반 구축**: 엑셀 입력 검토, 데이터 사전·스키마 확정, Python 생성기로 정상·이상·정비이력·원가 더미데이터 생성, 품질검증 | **35%** | 재현 가능한 더미데이터와 QA 결과 완성 |
| 2 | **KPI·이상 분석 엔진**: KPI 집계, 정상 기준, 규칙 기반 이상 후보 탐지, 영향 변수·추정 원인·원가 영향 계산 | **25%** | 이상 이벤트별 탐지·원인 후보·원가 계산 결과 확인 |
| 3 | **사례 추천·Copilot MVP**: 구조화된 과거 사례 유사도 추천, 근거 포함 질의응답, Morning Brief, 계획/목표 대비 실적 분석 | **25%** | 사용자가 대표 질문을 수행하고 답변을 확인 가능 |
| 4 | **시연·검증·발표**: 대표 이상 시나리오의 전 과정 시연, 결과 검증, 한계·보안 원칙 명시, 발표자료 작성 | **15%** | 데모와 발표자료 완성 |

**ML 적용은 1~4단계 완료 후에만 결정하는 선택 고도화 단계다.** 기본 과제 완료 조건에는 포함하지 않으며, 남은 Codex 사용량·일정·결과 품질을 확인한 뒤 모델 1개(이상 점수 탐지 또는 이상 유형 분류)만 추가할지 결정한다.

### 이번 과제의 실시간 연동 범위

- 이번 MVP는 생성된 더미데이터 파일을 분석·시연하는 방식으로 구현한다.
- 실제 Historian/MES/API 연동, 스트리밍, 폴링, 실시간 알림, 가상 실시간 재생은 구현하지 않는다.
- 변수명·시간·단위를 표준화하고, 실제 데이터 입력부로 교체 가능한 향후 확장 구조는 발표 로드맵에서만 제시한다.

### ML 학습·분석 기간 원칙

- 더미데이터는 총 2년치로 생성한다.
- ML을 추가하기로 결정하면 과거 1년은 ML 학습·보정용, 최근 1년은 학습에 사용하지 않은 평가·시연용으로 구분한다.
- KPI 추세, 대시보드, 규칙 기반 이상 탐지, 원가·사례 분석은 전체 2년 데이터에 적용한다.
- ML 이상 점수는 추가 결정 시 전체 기간에 계산·표시할 수 있으나, 모델 성능 설명과 대표 시연은 최근 1년 평가 구간을 기준으로 한다.

### 즉시 다음 작업

1. 사용자가 엑셀 템플릿을 작성한다.
2. Codex가 입력값의 누락·모순·우선순위를 검토한다.
3. 더미데이터의 스키마·생성 규칙·이상 삽입 규칙을 설계한다.
4. 재현 가능한 Python 생성기를 작성하고 결과를 검증한다.

### 계약·계획 데이터 추가 원칙

- 계약·경영계획 더미데이터는 조업 데이터 기반 MVP가 완성된 뒤 별도 모듈로 추가한다.
- 시간 단위 조업 데이터에 계약 항목을 섞지 않는다. 계약·계획 데이터는 월/분기 단위의 별도 테이블로 만들고, `KPI_ID`와 기간을 기준으로 실적과 연결한다.
- 계약·계획 기능을 시작할 시점에 Codex가 필요한 최소 입력 항목을 먼저 안내하고 사용자에게 요청한다.

### 역할 분담

| 단계 | 사용자(현업 판단·과제 방향) | Codex(설계·구현·산출물) |
| --- | --- | --- |
| 데이터 입력 | KPI·변수·공정 관계·이상 흐름을 익명화해 엑셀에 입력 | 템플릿 보완, 누락·모순·우선순위 검토 |
| 더미데이터 | 정상/이상 패턴이 공정 상식에 맞는지 검토·승인 | 데이터 스키마·생성 규칙 설계, Python 생성기 작성·실행, 데이터·QA 결과 생성 |
| 분석 로직 | 이상 판단 기준·점검 우선순위의 현업 타당성 확인 | KPI·이상탐지·원가·유사사례 계산 로직 구현 |
| Copilot/MVP | 필요한 질문과 원하는 업무 흐름을 정의, 응답 품질 검토 | 화면·질의응답·Morning Brief 구현 및 대표 시나리오 시연 구성 |
| 발표·검증 | 발표 메시지, 현업 관점의 한계·효과 확인 | 스토리라인·슬라이드 초안·도표·데모 자료 제작 |

## 10. Codex 사용량 절감 운영 원칙

- 대화에서 8,760행 수치 데이터를 직접 생성·수정하지 않는다. Codex가 생성기 작성과 최초 데이터 생성을 맡고, 대량 수치 생성·재생성은 로컬 Python 코드로 실행한다.
- 입력 수정은 엑셀에 모아서 한 번에 반영한 뒤 검토를 요청한다. 작은 변경마다 대화를 새로 시작하지 않는다.
- 생성기, 설정 파일, 데이터 사전, 시나리오 목록, 품질검증 보고서를 프로젝트 파일로 남긴다. 새 채팅·새 계정에서도 파일을 읽어 바로 이어간다.
- 단계별 완료 기준을 분리한다: `입력 검토 → 설계 승인 → 생성기 구현 → 생성·품질검증 → 분석/MVP → 발표자료`.
- 매 단계는 필요한 파일과 검토 기준을 명시한 짧은 요청으로 진행한다. 같은 파일을 동시에 여러 Codex 작업이 수정하지 않게 한다.
- 발표자료는 검증된 최종 데이터·분석 결과가 나온 뒤 별도 단계로 만든다. 중간 시안 반복을 최소화한다.

## 11. 새 작업에서 사용할 요청문

> `인수인계.md`와 `엑셀 양식/coke_oven_gas_dummy_data_input_template.xlsx`를 먼저 읽어줘. Coke Oven Gas 단일 라인 AI Copilot 과제이며, 현재 최우선 목표는 보안 원칙을 지키는 1년치 KPI 중심 더미데이터 설계·생성이다. 엑셀 입력을 검토해 누락·모순·우선순위를 먼저 알려주고, 승인 후 생성 규칙과 Python 생성기를 설계해줘.
