# 프로젝트 발표자료 작성 기준

## 프로젝트명

공정 운영 통합 분석 및 의사결정 지원 시스템

화면명은 “화성공장 공정 운영 분석 시스템”이며,
관리기준·이상징후·손익 영향을 하나의 흐름에서 분석하는 웹 기반 시스템이다.

---

## 1. 프로젝트를 시작한 이유

현업에서는 공정 상태를 판단하기 위해 MES 등의 여러 화면에서 기별·설비별 데이터를 각각 확인해야 한다.

이후에도 다음 내용을 따로 대조해야 한다.

- KPI가 관리기준을 벗어났는지
- 관련 공정 변수는 어떻게 변했는지
- 어떤 항목을 먼저 점검해야 하는지
- 가스·약품·스팀 사용량 변화가 손익에 어떤 영향을 주는지

즉, 문제는 단순히 “데이터가 여러 곳에 있다”는 것이 아니라,
공정 판단에 필요한 KPI·관리기준·관련 변수·손익 영향이 분산돼 있어 하나의 판단 흐름으로 연결하기 어렵다는 점이다.

또한 위탁사업 운영 환경에서는 사내망 데이터를 외부 개발 환경으로 옮겨 활용하는 데 제약이 있다.
따라서 이 과제는 실제 공정 데이터를 외부로 반출해 분석한 시스템이 아니라,
데이터 활용 제약 속에서도 분석 로직과 사용자 판단 흐름을 검증할 수 있도록 구현한 시스템이다.

---

## 2. 해결 방향

사용자가 기간을 선택하면 아래 흐름으로 판단할 수 있도록 구현했다.

기간 선택
→ KPI 및 관리기준 이탈 확인
→ 관련 변수와 이상 이벤트 확인
→ 점검 후보 우선순위 제시
→ 가스·약품·스팀 손익 영향 확인
→ AI 분석 브리핑

AI가 실제 원인을 확정하는 구조가 아니다.
관리기준 이탈, 변화량, 상관관계, 설정된 변수 관계를 계산한 뒤,
현업이 먼저 확인할 항목을 “점검 후보” 또는 “확인 우선순위”로 제시한다.

---

## 3. 합성 데이터 설계

실제 공정 원천 데이터를 외부 개발 환경에서 직접 쓰기 어려운 점을 고려해,
단순 랜덤 더미 데이터가 아닌 공정 특성을 반영한 합성 데이터를 직접 설계했다.

반영한 요소:

- 변수별 분포와 변동성
- KPI와 공정 변수 간 상관관계 방향
- 원인 변수 변화 후 대상 KPI가 반응하는 시간 지연
- 정상 → 전조 → 이상 → 회복 흐름
- 적용 시점이 있는 관리기준
- 가스·약품·스팀의 목표 사용량과 가상 단가

데이터 규모:

- 시간 단위 합성 데이터: 17,544행
- 일별 KPI 데이터: 731행
- 기준 기간: 2024-01-01 07:00 ~ 2026-01-01 06:00
- 조업일 기준: 07:00 ~ 다음 날 07:00
- 이상 이벤트: 5개

데이터 품질 검증 결과:

- 결측 및 중복 검사: PASS
- KPI 산식 재계산: PASS
- 변수 관계 방향 검사: PASS
- 이상 시나리오 시간 지연 검사: PASS
- 정상·전조·이상·회복 이벤트 흐름 검사: PASS

이 부분은 단순한 더미 데이터가 아니라,
공정 관계·계산 산식·시간 흐름을 반영해 분석 로직을 검증하기 위한 데이터 환경을 직접 설계한 점이 핵심이다.

---

## 4. 데이터 처리와 분석 방식

Python과 pandas DataFrame으로 시간 단위 데이터를 조업일 기준으로 집계했다.

주요 처리 내용:

- 오전 07:00 이전 데이터는 전날 조업일로 귀속
- 시간 단위 데이터를 일별 KPI로 집계
- 생산량·투입량을 기반으로 원단위 KPI 계산
- 약품 원단위 계산
- 기간별 KPI 평균 계산
- 관리기준 대비 편차와 목표 대비 편차 계산
- 이상 기간의 관련 변수 변화량과 실제 상관관계 계산

점검 후보 산정 방식:

1. 선택 기간의 데이터를 조회한다.
2. 대상 KPI의 관리기준 이탈 여부를 계산한다.
3. 관련 후보 변수의 직전 값 대비 변화율을 계산한다.
4. 대상 KPI와 후보 변수의 Pearson 상관계수를 계산한다.
5. 설정한 관계 방향과 실제 상관 방향이 일치하는지 확인한다.
6. 상대 변화율 × 상관계수 절대값 × 가중치로 후보를 정렬한다.

중요한 사실:

- 학습형 ML 모델을 구현한 것은 아니다.
- 검증된 고장 라벨 데이터가 충분하지 않은 상태에서, 모델 결과를 과장하지 않고 계산 근거가 보이는 분석 방식을 선택했다.
- 분석 결과는 실제 원인이 아니라 점검 후보와 확인 우선순위다.

---

## 5. 손익 영향 분석

가스량, 약품, 스팀의 실제 사용량과 목표 사용량 차이를 계산하고,
가상 단가를 적용해 기간별 손익 영향을 계산했다.

계산 흐름:

시간 단위 실제 사용량
→ 월별 목표량을 시간 단위 목표량으로 배분
→ 실제 사용량과 목표 사용량의 차이 계산
→ 가상 단가 적용
→ 가스·약품·스팀별 기간 합계와 상세 추이 표시

가스는 실제 사용량과 목표 사용량 차이에 가상 단가를 적용한다.
약품과 스팀은 목표 대비 사용량 감소가 절감 방향으로 계산되도록 산식의 부호 방향이 다르다.

중요:

화면의 손익 수치는 실제 비용 절감 성과가 아니다.
반드시 “합성 데이터·가상 단가 기준 손익 영향”으로 표기해야 한다.

---

## 6. IT 구현 구조

프런트엔드:

- HTML, CSS, Vanilla JavaScript
- 통합 운영현황
- AI 분석 브리핑
- 공정 분석 현황
- 이상 분석·점검
- 손익 영향 분석
- 공정 데이터 비교 분석
- 관리기준 체계

백엔드:

- Node.js 내장 HTTP 서버
- Copilot 분석 API
- 운영 데이터 API
- 관리기준 API

저장소:

- SQLite
- 운영 데이터
- 관리기준
- 기준 적용 버전
- 변수 관계와 가중치
- 데이터 버전
- 관리기준 버전
- Copilot 분석 결과 재사용 정보 저장

MCP:

- 기간 분석
- 운영 데이터 조회·등록·삭제
- 관리기준 조회·변경·삭제
- 변수 관계와 가중치 조회·변경

MCP는 “AI가 기간 분석·관리기준 조회 같은 기능을 도구로 호출하는 연결 방식”으로 설명한다.

---

## 7. 신규 데이터 반영 구조

신규 운영 데이터는 API 또는 MCP로 등록할 수 있다.
관리기준도 적용일 기준으로 등록·변경할 수 있다.

운영 데이터가 바뀌면 data_version이 증가한다.
관리기준이 바뀌면 standards_version이 증가한다.

Copilot 분석 결과는 조회 기간, 대상 KPI, 데이터 버전, 관리기준 버전을 기준으로 관리한다.
따라서 데이터나 기준이 바뀌면 이전 결과를 그대로 쓰지 않고 새 분석 결과를 만든다.

중요한 구현 범위:

- Copilot 분석은 SQLite와 API를 사용한다.
- 통합 운영현황·공정 분석·손익 화면의 상당 부분은 현재 프런트엔드 정적 데이터 모델을 기반으로 동작한다.
- 따라서 “Copilot 분석의 신규 데이터·기준 반영 구조를 구현했다”는 표현은 가능하다.
- 그러나 “모든 화면이 신규 DB 데이터를 실시간 자동 반영한다”고 말하면 안 된다.

---

## 8. LLM 사용 방식

NVIDIA NIM 연동 코드를 구현했다.
NVIDIA API 키와 모델 설정이 있는 경우, 첫 분석 결과 생성 시 외부 LLM 호출을 시도한다.

LLM의 역할은 분석 결과를 읽기 쉬운 문장으로 정리하는 것이다.

내부 계산이 먼저 수행하는 내용:

- 관리기준 이탈
- 기준 대비 편차
- 목표 대비 편차
- 관련 변수 변화량
- 실제 상관관계
- 점검 후보 우선순위

LLM이 수행하지 않는 내용:

- 실제 원인 확정
- KPI 계산
- 손익 계산
- 설비 운전 판단

AI 브리핑의 출력 항목:

- 예상 원인
- 영향 KPI
- 점검 우선순위
- 요약 설명

안정성 및 반복 호출 축소 방식:

- 같은 기간·같은 대상 KPI·같은 데이터 버전·같은 관리기준 버전이면 기존 분석 결과를 재사용한다.
- LLM 호출 전에도 계산 기반 Brief를 만든다.
- NVIDIA API 키 또는 모델 설정이 없으면 외부 호출 없이 계산 기반 설명을 사용한다.
- API 오류, 네트워크 오류, 시간 초과, JSON 형식 오류가 나도 계산 기반 설명을 제공한다.

표현 방식:

“LLM 비용이 없다”라고 쓰지 않는다.

대신 다음처럼 쓴다.

“동일 조건의 분석 결과를 재사용해 반복 호출을 줄였고,
외부 모델 호출이 실패해도 계산 기반 설명으로 분석 흐름이 중단되지 않도록 설계했다.”

---

## 9. 실제 화면별 기능

통합 운영현황:

- 선택 기간 KPI 상태
- 정상·주의·이상 상태
- 관리기준 대비 및 목표 대비 편차
- 가스·약품·스팀 손익 영향 요약
- AI 브리핑, 이상 분석, 손익 분석 화면 이동

AI 분석 브리핑:

- 대상 KPI 상태
- 관리기준 대비 수준
- 점검 우선순위
- AI 설명 또는 계산 기반 설명
- 확정 원인이 아닌 데이터 기반 점검 후보라는 안내

이상 분석·점검:

- 이상 이벤트
- 관리기준 이탈 근거
- 관련 지표 추이
- 예상 원인·점검 후보
- 권장 점검 항목

손익 영향 분석:

- 가스·약품·스팀별 실제 사용량과 목표 사용량
- 기간별 손익 영향
- 항목별 상세 추이
- 합성 데이터·가상 단가 기준 결과

공정 데이터 비교 분석:

- 기간별 지표 비교
- 조업 관리기준 대비 분석
- 손익 KPI 기준 대비 분석

관리기준 체계:

- 정상·주의·이상 기준
- 목표값
- 적용일
- 기준 변경 이력
- 판정 규칙

---

## 10. 실제 구현 결과와 한계

구현한 것:

- 합성 데이터 생성과 QA
- DataFrame 기반 조업일 집계
- 기간별 KPI·기준·손익 분석 화면
- 관리기준과 상관관계 기반 점검 후보 분석
- SQLite 저장소
- API와 MCP
- NVIDIA NIM 연동 코드
- 계산 기반 fallback
- 동일 분석 조건 결과 재사용

아직 구현하지 않은 것:

- 실제 MES/PI/Historian 자동 연계
- 실제 공정 데이터 기반 검증
- 실제 원가 절감액 산정
- 모든 화면의 SQLite API 단일 데이터 원천화
- 실시간 알림
- 정비 이력 기반 유사 사례 추천

향후 계획:

PI 데이터 연계 이후 실제 데이터를 연결하고,
현업 검증을 통해 관리기준·변수 관계·가중치를 보정한다.

---

## 11. 발표에서 반드시 전달할 메시지

1. 여러 화면에 나뉜 KPI·관리기준·손익 정보를 한 판단 흐름으로 연결했다.
2. 실제 데이터 활용 제약 때문에 단순 더미가 아니라 공정 특성을 반영한 합성 데이터와 QA 체계를 직접 설계했다.
3. 학습형 ML을 과장하지 않고, 관리기준·변화량·상관관계라는 계산 근거로 점검 후보를 제시했다.
4. API·SQLite·버전 관리·MCP로 신규 데이터와 기준 변경을 고려한 구조를 구현했다.
5. LLM은 분석을 대신하지 않고 설명 역할로 제한했으며, 결과 재사용과 fallback을 구현했다.
6. 현재는 합성 데이터로 구조를 검증한 단계이며, PI 데이터 연계 이후 실제 데이터 기반 현업 검증으로 확장할 수 있다.

---

## 12. 발표에서 금지할 표현

- MVP
- 실제 절감 성과
- 실제 원인을 AI가 진단·확정
- MES/PI 자동 연계 완료
- 학습형 ML 모델 구현
- 모든 화면의 실시간 신규 데이터 자동 반영
- LLM 비용 없음

대신 사용할 표현:

- 합성 데이터·가상 단가 기준 손익 영향
- 점검 후보
- 확인 우선순위
- 동일 조건 결과 재사용으로 반복 호출 축소
- 호출 실패 시 계산 기반 설명
- PI 데이터 연계 이후 실제 데이터 기반 현업 검증
