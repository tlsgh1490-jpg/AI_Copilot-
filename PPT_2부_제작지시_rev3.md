# 발표자료 제작 지시서

당신은 임원 보고자료와 AI 교육 최종발표 자료를 만드는 프레젠테이션 전문가입니다.

함께 제공하는 PPT_1부_프로젝트_내용_rev3.md를 프로젝트 사실관계의 기준 문서로 사용하세요.
1부에 없는 기능, 수치, 성과, 연계 상태를 추측해 추가하지 마세요.

---

## 1. 제작 목표

- 7분 발표용 PowerPoint 7장을 제작합니다.
- 청중은 임원과 비전공자를 포함합니다.
- 기술 용어는 사용하되 괄호 안에 쉬운 설명을 붙입니다.
- 발표자가 자료를 읽는 수준으로 진행해도 흐름이 자연스럽게 이어져야 합니다.
- 결과 화면을 나열하는 자료가 아니라, 문제를 어떻게 정의했고 데이터 제약 속에서 어떤 고민과 설계를 거쳐 구현했는지를 보여 줍니다.
- 페이지마다 우측 하단에 1 / 7, 2 / 7 형식의 페이지 번호를 넣습니다.
- 각 페이지의 발표자 노트에 1분 이내의 읽기 쉬운 발표 대본을 작성합니다.

발표 제목:

공정 운영 통합 분석 및 의사결정 지원 시스템

화면명:

화성공장 공정 운영 분석 시스템

---

## 2. 발표 전체 구조

각 페이지 제목 앞에 아래 형식의 작은 구분 키워드를 넣습니다.

[구분 키워드] 결론이 드러나는 본제목

전체 흐름은 다음과 같습니다.

[프로젝트 개요] → [문제 정의] → [AI 과제 구현] → [결과·향후계획]

3~6장은 모두 [AI 과제 구현]으로 묶어, 해결 방향·데이터 설계·분석 로직·IT 구조·LLM 적용을 하나의 과제 수행 내용으로 보여 줍니다.

---

## 3. 7장 구성과 페이지별 제작 지시

### 1장 — 프로젝트 개요

제목:

[프로젝트 개요] 공정 운영 통합 분석 및 의사결정 지원 시스템

넣을 내용:

- 부제: 관리기준·이상징후·손익 영향을 하나의 판단 흐름으로 연결
- 화면명: 화성공장 공정 운영 분석 시스템
- 발표자 성명

구성:

- 표지는 제목, 부제, 성명만 배치합니다.
- 기술 스택이나 기능 목록을 표지에 넣지 않습니다.

발표 대본 방향:

여러 화면에 흩어진 공정 정보를 하나의 판단 흐름으로 연결하고, 데이터 제약 속에서도 분석 로직과 사용자 경험을 검증한 과제라는 메시지로 시작합니다.

---

### 2장 — 문제 정의

제목:

[문제 정의] 분산된 공정 정보 확인으로 누적되는 판단 시간

반드시 보여 줄 문제 흐름:

MES 여러 화면에서 기별·설비별 데이터 확인
→ KPI와 관리기준 대조
→ 관련 변수 변화 확인
→ 가스·약품·스팀 손익 영향 별도 확인
→ 현업이 종합 판단

강조할 내용:

- 문제는 데이터가 단순히 여러 곳에 있다는 것이 아닙니다.
- KPI·관리기준·관련 변수·손익 영향이 분산되어 하나의 판단 흐름으로 연결하기 어렵다는 점이 핵심입니다.
- 위탁사업 환경에서는 포스코 사내망 데이터를 외부 개발 환경으로 옮겨 활용하기 어렵다는 제약이 있습니다.
- 따라서 실제 공정 데이터를 반출해 성과를 증명한 과제가 아니라, 제약 속에서 분석 구조와 판단 흐름을 검증한 과제입니다.

구성:

- 실제 화면 캡처를 여러 장 넣지 않습니다.
- 3~5단계 흐름 도식으로 표현합니다.
- 각 단계 아래에 확인 화면 분산, 대조 작업, 판단 지연처럼 짧은 설명만 넣습니다.

발표 대본 방향:

왜 이 과제를 시작했는지, 현업에서 무엇이 불편했는지, 데이터 반출 제약이 왜 중요한지 설명합니다.

---

### 3장 — 합성 데이터 설계 및 검증

제목:

[AI 과제 구현] 공정 특성을 반영한 합성데이터 검증 환경

반드시 드러낼 노력:

- 실제 공정 원천 데이터를 외부에서 직접 사용하기 어려워 단순 랜덤 더미를 사용하지 않았습니다.
- 변수별 분포와 변동성을 고려했습니다.
- KPI와 공정 변수의 상관관계 방향을 반영했습니다.
- 원인 변수 변화 후 KPI가 반응하는 시간 지연을 반영했습니다.
- 정상 → 전조 → 이상 → 회복 흐름을 만들었습니다.
- 적용 시점이 있는 관리기준과 가스·약품·스팀 목표량·가상 단가를 반영했습니다.

근거 수치:

- 시간 단위 합성 데이터 17,544행
- 일별 KPI 데이터 731행
- 2024-01-01 07:00 ~ 2026-01-01 06:00
- 조업일 기준 07:00 ~ 다음 날 07:00
- 이상 이벤트 5개

QA 결과:

- 결측 및 중복 검사 PASS
- KPI 산식 재계산 PASS
- 변수 관계 방향 검사 PASS
- 이상 시나리오 시간 지연 검사 PASS
- 정상·전조·이상·회복 흐름 검사 PASS

구성:

- 왼쪽: 데이터 설계 요소를 4~5개 키워드로 표현합니다.
- 가운데: 정상→전조→이상→회복 시간 흐름을 표시합니다.
- 오른쪽: 수치와 QA PASS를 크게 보여 줍니다.
- 더미데이터라는 표현만 크게 쓰지 말고, 공정 특성 반영 합성데이터라고 표기합니다.

발표 대본 방향:

데이터를 구하지 못했다는 변명이 아니라, 실제 데이터 없이도 분석 로직을 검증할 수 있도록 공정 관계와 시간 흐름까지 직접 설계했다는 점을 설명합니다.

---

### 4장 — 분석 로직 및 판단 기준

제목:

[AI 과제 구현] 관리기준과 데이터 관계를 결합한 점검 우선순위

반드시 보여 줄 4단계:

기간 집계
→ 관리기준 이탈 확인
→ 변화량·상관관계 계산
→ 점검 후보 우선순위 제시

실제 처리 내용:

- Python과 pandas DataFrame으로 시간 단위 데이터를 조업일 기준으로 집계
- 오전 07:00 이전 데이터는 전날 조업일로 귀속
- 생산량·투입량 기반 원단위 KPI 계산
- 기간별 KPI 평균과 기준 대비 편차 계산
- 후보 변수의 직전 값 대비 변화율 계산
- KPI와 후보 변수의 Pearson 상관계수 계산
- 설정한 관계 방향과 실제 상관 방향이 일치하는지 확인
- 상대 변화율 × 상관계수 절대값 × 가중치로 후보 정렬

반드시 넣을 주의 문구:

AI가 실제 원인을 확정하는 것이 아니라, 데이터 근거가 있는 확인 우선순위를 제시합니다.

구성:

- 수식 전체를 어렵게 나열하지 않습니다.
- 기준을 벗어났는가 → 무엇이 함께 변했는가 → 무엇부터 볼 것인가로 쉽게 설명합니다.
- 학습형 ML 모델을 구현했다고 표현하지 않습니다.

발표 대본 방향:

라벨 데이터가 충분하지 않은 상황에서 결과를 과장하지 않고, 관리기준·변화량·상관관계라는 계산 근거를 선택한 이유를 설명합니다.

---

### 5장 — 시스템 구조 및 신규 데이터 반영

제목:

[AI 과제 구현] 신규 데이터 반영을 고려한 API·SQLite·MCP 구조

도식:

운영 데이터·관리기준 입력
→ SQLite 저장 및 버전 관리
→ 분석 API·Copilot 분석
→ 통합 운영현황·분석 화면

기술 내용:

- 프런트엔드: HTML, CSS, Vanilla JavaScript
- 백엔드: Node.js 내장 HTTP 서버
- 저장소: SQLite
- API: 운영 데이터 API, 관리기준 API, Copilot 분석 API
- MCP: 기간 분석·운영 데이터·관리기준·변수 관계를 AI가 도구로 호출하는 연결 방식

신규 데이터 반영을 반드시 설명:

- 신규 운영 데이터는 API 또는 MCP로 등록할 수 있습니다.
- 관리기준은 적용일 기준으로 등록·변경할 수 있습니다.
- 운영 데이터가 바뀌면 data_version이 증가합니다.
- 관리기준이 바뀌면 standards_version이 증가합니다.
- 분석 결과는 조회 기간·대상 KPI·데이터 버전·관리기준 버전을 기준으로 관리합니다.
- 데이터나 기준이 바뀌면 이전 결과를 그대로 쓰지 않고 새 분석 결과를 만듭니다.

한계도 정확히 표시:

- Copilot 분석은 SQLite와 API를 사용합니다.
- 통합 운영현황·공정 분석·손익 화면의 상당 부분은 현재 프런트엔드 정적 데이터 모델 기반입니다.
- 모든 화면이 신규 DB 데이터를 실시간 자동 반영한다고 표현하지 않습니다.
- 실제 MES/PI 자동 연계는 현재 완료가 아니라 향후 단계입니다.

구성:

- 계층형 구조도 한 장으로 표현합니다.
- MCP 옆에 반드시 AI가 기간 분석·관리기준 조회 같은 기능을 도구로 호출하는 연결 방식이라는 쉬운 주석을 넣습니다.

발표 대본 방향:

단순히 화면을 만든 것이 아니라, 데이터와 기준이 바뀌는 상황을 고려해 등록·버전·재분석 구조까지 설계했다는 점을 설명합니다.

---

### 6장 — LLM 적용 및 안정성

제목:

[AI 과제 구현] 계산 근거 기반 AI 브리핑과 반복 호출 최소화

왼쪽:

- 실제 AI 분석 브리핑 화면을 한 장 크게 배치합니다.
- 화면 속 예상 원인·영향 KPI·점검 우선순위·요약 설명을 읽을 수 있는 크기로 보여 줍니다.

오른쪽에는 다음 4개 원칙만 표시합니다.

1. 계산 근거 먼저
2. LLM은 설명 역할
3. 같은 조건 결과 재사용
4. 호출 실패 시 계산 기반 설명

반드시 설명할 LLM 역할:

- NVIDIA NIM 연동 코드를 구현했습니다.
- 내부 계산이 관리기준 이탈, 편차, 변화량, 상관관계, 점검 후보 순위를 먼저 계산합니다.
- LLM은 이 결과를 읽기 쉬운 문장으로 정리합니다.
- LLM이 실제 원인, KPI, 손익, 설비 운전을 확정하거나 계산하지 않습니다.

비용과 안정성에 대한 고민:

- 동일 기간·대상 KPI·데이터 버전·관리기준 버전이면 기존 분석 결과를 재사용해 반복 호출을 줄입니다.
- API 키나 모델 설정이 없으면 외부 호출 없이 계산 기반 설명을 사용합니다.
- API 오류·네트워크 오류·시간 초과·JSON 오류가 나도 계산 기반 설명을 제공합니다.
- LLM 비용이 없다고 표현하지 않습니다.
- 대신 동일 조건 결과 재사용으로 반복 호출을 줄이고, 호출 실패에도 분석이 중단되지 않도록 설계했다고 표현합니다.

발표 대본 방향:

LLM을 무조건 많이 호출한 것이 아니라, 계산과 설명의 역할을 분리하고 비용·장애·재현성을 고려해 사용 범위를 제한했다는 점을 설명합니다.

---

### 7장 — 결과 및 향후계획

제목:

[결과·향후계획] 통합 분석 화면 구현과 PI 데이터 연계 이후 현업 검증

왼쪽:

- 실제 통합 운영현황 화면을 크게 배치합니다.
- 화면 안의 KPI 상태, 관리기준 편차, 손익 요약이 읽히는 크기를 유지합니다.

오른쪽 세로 흐름:

기간 선택
→ KPI·관리기준 이탈 확인
→ 점검 후보 확인
→ 손익 영향 확인
→ AI 분석 브리핑

손익 수치 주석:

합성 데이터·가상 단가 기준 손익 영향

현재 구현 결과:

- 합성 데이터 생성과 QA
- DataFrame 기반 조업일 집계
- 기간별 KPI·기준·손익 분석 화면
- 관리기준·상관관계 기반 점검 후보 분석
- SQLite, API, MCP
- NVIDIA NIM 연동 코드
- 계산 기반 fallback
- 동일 분석 조건 결과 재사용

향후 계획은 한 줄로 제한:

PI 데이터 연계 이후 실제 데이터 기반 현업 검증·관리기준·변수 관계·가중치 보정

구현하지 않은 기능을 성과처럼 쓰지 않습니다:

- 실제 MES/PI/Historian 자동 연계
- 실제 공정 데이터 검증
- 실제 원가 절감액 산정
- 모든 화면의 단일 실시간 DB 연동
- 실시간 알림
- 정비 이력 기반 유사 사례 추천

발표 대본 방향:

현재는 합성 데이터로 구조와 판단 흐름을 검증한 단계이며, PI 연계 이후 실제 데이터와 현업 검증을 통해 기준과 변수 관계를 보정한다는 결론으로 마무리합니다.

---

## 4. 손익 영향 표현 기준

가스·약품·스팀 수치는 실제 절감액이 아닙니다.

반드시 다음 표현을 사용합니다.

합성 데이터·가상 단가 기준 손익 영향

다음 표현은 사용하지 않습니다.

- 실제 비용 절감
- 절감 성과
- 원가 절감액 달성

---

## 5. 디자인 및 가독성 기준

- 제공된 회사 PPT 양식을 그대로 사용합니다.
- 양식의 글꼴, 색상, 여백, 제목·본문 위계를 유지합니다.
- 임의의 새 테마, 과도한 그라데이션, 큰 카드형 박스를 만들지 않습니다.
- 한 슬라이드의 핵심 메시지는 하나만 둡니다.
- 긴 문단 대신 흐름도, 짧은 키워드, 실제 화면, 근거 수치를 사용합니다.
- 실제 화면은 6~7장에 크게 사용하고, 작은 캡처 여러 장을 나열하지 않습니다.
- 기술 용어 옆에 쉬운 설명을 붙입니다.
- 모든 페이지에 n / 7 페이지 번호를 넣습니다.
- 발표자 노트는 전문용어를 그대로 읽어도 이해되도록 1분 이내의 구어체 대본으로 작성합니다.
- 제목은 반드시 [상위 구분] 결론형 본제목 형식을 사용합니다.

---

## 6. 금지 표현

- MVP
- Coke Oven GAs
- 실제 절감 성과
- 실제 원인을 AI가 진단·확정
- MES/PI 자동 연계 완료
- 학습형 ML 모델 구현
- 모든 화면이 신규 DB를 실시간 자동 반영
- LLM 비용 없음

대체 표현:

- 공정 특성 반영 합성데이터
- 점검 후보
- 확인 우선순위
- 합성 데이터·가상 단가 기준 손익 영향
- 동일 조건 결과 재사용으로 반복 호출 축소
- 호출 실패 시 계산 기반 설명
- PI 데이터 연계 이후 실제 데이터 기반 현업 검증

---

## 7. 제출 파일

1. 회사 PPT 양식 기반 7장 PPTX
2. 페이지별 발표자 노트·대본이 포함된 PPTX
3. 실제 시연 영상은 별도 MP4 파일로 제출하며, 7장에 시연 영상 별도 MP4 제출을 표시합니다.

PPT 제작자는 1부의 사실관계를 벗어나지 않으면서, 위의 노력·의사결정·제약·한계·향후 확장 내용을 화면과 대본에 반영해야 합니다.
